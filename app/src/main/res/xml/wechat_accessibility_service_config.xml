<?xml version="1.0" encoding="utf-8"?>
<!--
    SPDX-License-Identifier: GPL-3.0-only

    Diagnostic configuration for WeChatCallAccessibilityService.

    - No packageNames restriction here: overlay-style windows (which WeChat's call UI may be)
      sometimes aren't reliably tagged with their owning package on the event itself, so
      filtering at this level can silently drop the exact events we care about. Filtering by
      package is instead done in code, by checking each window's own root node package name
      after enumerating all currently visible windows.
    - accessibilityEventTypes: window state changes, content changes, and window-list changes
      (the last one matters for overlay windows appearing/disappearing, e.g. an incoming call
      screen drawn as a floating layer rather than a normal Activity).
    - canRetrieveWindowContent: required to read the visible text on WeChat's screen.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100" />
